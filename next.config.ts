/** @type {import('next').NextConfig} */
const nextConfig: import("next").NextConfig = {
  images: {
    domains: [
      "www.publicdomainpictures.net",
      "www.thesprucepets.com",
      "upload.wikimedia.org",
      "hdqwalls.com",
      "lafeber.com",
    ], // Add external domains for images
  },
};

export default nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
